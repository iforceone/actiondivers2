export const liveReservationRequestsEnabled =
  import.meta.env.VITE_RESERVATION_REQUESTS_ENABLED === 'true';

export const reservationUnavailableMessage =
  'Online request saving is not available in this preview. Your selections remain on this device; send them through WhatsApp and staff can help finish the request.';
