{
  "name": "actiondivers-inquiry-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config wrangler.toml",
    "deploy": "wrangler deploy --config wrangler.toml",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260727.1",
    "wrangler": "^4.114.0"
  },
  "dependencies": {
    "@google/genai": "^1.52.0"
  }
}
